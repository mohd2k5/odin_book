<h1>User Search</h1>

<% if @users.any? %>
  <% @users.each do |user| %>
    <div class="user-card">
      <%= link_to user.user_name, user_path(user) %>
    </div>
  <% end %>
<% else %>
  <p>No users found.</p>
<% end %>